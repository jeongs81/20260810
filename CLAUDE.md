# LLM Wiki SSOT — 개발 규칙

## 핵심 원칙

1. **SSOT는 Raw에만** — Wiki는 파생 아티팩트
2. **액션 중심 설계** — 결정/액션/보류로 구성
3. **최소 구조 강제** — 프론트매터만 필수, 본문 자유
4. **파일명 규약** — `YYYY-MM-DD-[회의체명].md`

---

## ✅ Raw 회의록 작성 규칙

### 필수
1. 프론트매터 포함 (`회의일`, `참석`)
2. 파일명: `YYYY-MM-DD-[회의체명].md`
3. 결정을 `### 결정` 헤더로 명시
4. 액션을 `## 액션 아이템`에 체크박스로 기록
5. **이전 액션의 상태를 모두 업데이트**
6. 우선순위 변경 시 사유 명시
7. 외부 의존성 표시
8. 보류 항목에 해결 조건 기록

---

## ❌ 절대 피할 것

### Raw에서
- ❌ 이전 액션 상태 기록 안 함
- ❌ 프론트매터 없음
- ❌ 결정을 본문에 숨김
- ❌ 액션 상태 불명확
- ❌ 우선순위 변경 설명 없음
- ❌ 외부 의존성 숨김
- ❌ 보류 항목 방치

### Wiki 생성 및 검증 순서 (필수)
- ❌ **스킬 없이 수동으로 Wiki 생성하기** → 반드시 `/llm-wiki-ingest` 사용
- ❌ **기계 검증 전에 컨텐츠 검증 실행** → 반드시 순서대로
- ❌ **단계 건너뛰기** → 1단계 → 2단계 → 3단계 순서 필수
- ❌ **기계 검증 없이 컨텐츠 검증** → check.py 통과 후에만

### 일반
- ❌ Raw와 Wiki 불일치 방치
- ❌ 의사결정 배경 기록 안 함
- ❌ Git 커밋 미루기

---

## ⚠️ 가장 중요한 2가지

1. **이전 액션 상태 업데이트** (Raw)
   ```markdown
   ## 이전 액션 상태 업데이트
   - [x] 완료된 액션
   - ⏳ 진행 중인 액션
   - ⏸️ 홀드된 액션
   ```

2. **Raw 작성이 모든 것의 기초**
   - Wiki는 Raw에서 자동 생성됨
   - Raw의 품질이 Wiki의 품질을 결정
   - 규칙을 따르는 Raw 작성이 핵심

---

## 🤖 Wiki 생성 후 검증 워크플로우

**4단계 순차 프로세스**: Raw 적재 → 1단계(Wiki 생성) → 2단계(기계 검증) → 3단계(컨텐츠 검증)

⚠️ **핵심**: 각 단계는 이전 단계가 완료되어야만 시작 가능
- 1단계 시작: Raw 파일 적재 완료 필수
- 2단계 시작: 1단계 완료 (Wiki 파일 생성됨) 필수
- 3단계 시작: 2단계 통과 (check.py 오류 0개) 필수

---

### 1단계: Wiki 생성 (llm-wiki-ingest 스킬)

**진입 조건**:
- [ ] Raw 파일이 `raw/` 폴더에 있음
- [ ] 파일명: `YYYY-MM-DD-[회의체명].md`
- [ ] Frontmatter 완성됨 (회의일, 참석 필수)
- [ ] 결정/액션 섹션 작성됨

**명령**:
```bash
/llm-wiki-ingest raw/YYYY-MM-DD-[회의체명].md
```

**스킬이 처리**:
- Wiki 프론트매터 작성 (원본, 회의일, 참석 등)
- 결정/액션/보류 3계층 구조
- 액션 테이블 생성 (의존성 컬럼 포함)
- 고위험 액션 표시 (🚨)
- 이전 액션 추적 (완료/미해결 분리)
- 의존성 표시 (🟢/🔴/⚫)

**성공 기준**:
- [ ] Wiki 파일 생성됨
- [ ] Frontmatter 자동 작성
- [ ] 3계층 구조 생성

---

### 2단계: 기계 검증 (check.py 스크립트)

**진입 조건**:
- [ ] 1단계 완료됨 (Wiki 파일 생성됨)
- [ ] `wiki/YYYY-MM-DD-[회의체명].md` 파일이 존재
- [ ] Frontmatter가 자동 작성됨
- [ ] 3계층 구조(결정/액션/보류)가 생성됨

⚠️ **순서 규칙**: 1단계 완료 없이 이 단계 진행 금지

**명령**:
```bash
python check.py
```

**스크립트가 검증**:
- ✅ 파일명 규칙 (`YYYY-MM-DD-*.md`)
- ✅ Frontmatter 필드 (회의일, 참석 필수)
- ✅ 스키마 구조 (결정/액션/보류 섹션)
- ✅ 액션 테이블 컬럼 (`| # | 담당자 | 항목 | 마감 | 상태 | 의존성 |`)
- ✅ Frontmatter ↔ 파일명 일관성

**성공 기준**:
- [ ] 파일명 검증 통과
- [ ] Frontmatter 검증 통과
- [ ] 스키마 검증 통과
- [ ] 일관성 검증 통과
- [ ] 오류: 0개, 경고: 0개 (또는 사소한 수준)

**실패 시 대응**:
❌ 오류 발생 → Wiki 파일 수정 → 다시 실행

---

### 3단계: 컨텐츠 검증 (wiki-content-review 스킬)

**진입 조건**:
- [ ] 2단계 통과됨 (check.py 오류 0개)
- [ ] Wiki 파일이 기계 검증 통과
- [ ] Frontmatter, 파일명, 스키마 모두 정합적

⚠️ **순서 규칙**: 2단계 통과 없이 이 단계 진행 금지
⚠️ **오류 원칙**: 2단계에서 오류가 있으면 Wiki를 수정 후 재검증해야 함

**명령** (단일 파일):
```bash
/wiki-content-review wiki/YYYY-MM-DD-[회의체명].md
```

**명령** (일괄 검증 - 모든 파일이 2단계 통과 후):
```bash
/wiki-content-review all
```

**스킬이 검증**:
- 🔍 Raw ↔ Wiki 내용 일치도
- 🔍 임의 생성된 콘텐츠 감지
- 🔍 누락된 중요 내용 확인
- 🔍 액션 추적 (열림/진행/완료, 조용히 사라진 액션)
- 🔍 의존성 표시 (🟢/🔴/⚫)
- 🔍 고위험 표시 (🚨)

**검증 항목**:
- [ ] 결정사항이 Raw와 일치
- [ ] 액션 아이템이 Raw와 일치
- [ ] 이전 액션 상태가 추적됨
- [ ] 임의 생성 콘텐츠 없음
- [ ] 외부 의존성 표시됨
- [ ] 액션이 조용히 사라지지 않음

**성공 기준**:
- [ ] 통과 (오류 0개)
- [ ] 또는 경고만 있음 (경고는 확인 후 처리)

**실패 시 대응**:
⚠️  경고 발생 → Raw 또는 Wiki 수정 → 재검증
❌ 오류 발생 → Wiki 재생성 또는 Raw 수정

---

## 📋 완전한 워크플로우 체크리스트

⚠️ **중요**: 각 단계는 이전 단계 완료 후 진행. 단계 건너뛰기 금지!

---

### 0단계: Raw 적재
```
파일 위치: raw/YYYY-MM-DD-[회의체명].md
```
- [ ] 파일명: `YYYY-MM-DD-[회의체명].md` (정확한 형식)
- [ ] Frontmatter: 회의일, 참석 필수
- [ ] 결정/액션/보류 섹션 포함
- [ ] 이전 액션 상태 업데이트 포함
- [ ] Git 커밋 (Raw 적재 확인)

👉 **다음**: 1단계로 진행 (기계 생성)

---

### 1단계: Wiki 자동 생성 (llm-wiki-ingest 스킬)
```
/llm-wiki-ingest raw/YYYY-MM-DD-[회의체명].md
```
**진입 조건**:
- Raw 파일이 적재됨 ✅

**완료 기준**:
- [ ] Wiki 파일 생성: `wiki/YYYY-MM-DD-[회의체명].md` 생성됨
- [ ] Frontmatter 자동 작성: 원본, 회의일, 참석 등
- [ ] 3계층 구조: 결정/액션/보류 섹션 생성됨
- [ ] 액션 테이블: `| # | 담당자 | 항목 | 마감 | 상태 | 의존성 |` 형식

👉 **다음**: 2단계로 진행 (기계 검증)

---

### 2단계: 기계 검증 (check.py 스크립트)
```
python check.py
```
**진입 조건**:
- 1단계 완료됨 (Wiki 파일 생성됨) ✅

**완료 기준**:
- [ ] 파일명 검증 통과 ✅
- [ ] Frontmatter 검증 통과 ✅ (필수 필드 4개)
- [ ] 스키마 검증 통과 ✅ (액션 테이블 컬럼)
- [ ] 일관성 검증 통과 ✅ (파일명 ↔ 회의일 일치)
- [ ] **오류 0개 확인** (경고는 무시 가능)

**실패 시**:
- Wiki 파일 수정 후 `python check.py` 재실행
- 오류 0개까지 반복

👉 **다음**: 3단계로 진행 (컨텐츠 검증)

---

### 3단계: 컨텐츠 검증 (wiki-content-review 스킬)
```
/wiki-content-review wiki/YYYY-MM-DD-[회의체명].md
```
**진입 조건**:
- 2단계 통과됨 (check.py 오류 0개) ✅

**완료 기준**:
- [ ] Raw ↔ Wiki 내용 일치도 검증
- [ ] 임의 생성 콘텐츠 없음
- [ ] 누락된 중요 내용 없음
- [ ] 액션 추적 정확 (완료/미해결/조용히 사라짐 없음)
- [ ] 의존성 표시 정확 (🟢/🔴/⚫)
- [ ] 오류 0개 (경고는 검토 후 처리)

**경고 시**:
- 원인 분석 후 대응
- Raw 또는 Wiki 수정
- `/wiki-content-review ...` 재실행

**오류 시**:
- 1단계부터 다시 시작
- `/llm-wiki-ingest` 재실행

👉 **다음**: 최종 확인

---

### 최종 확인
- [ ] 1단계 완료 ✅ (Wiki 생성됨)
- [ ] 2단계 통과 ✅ (check.py 오류 0개)
- [ ] 3단계 통과 ✅ (wiki-content-review 통과)
- [ ] Git 커밋: 3개 파일 모두 포함
  - `wiki/YYYY-MM-DD-[회의체명].md` (생성됨)
- [ ] Git 푸시: GitHub 반영

---

## 🚨 주요 검증 규칙

### 1단계 완료 후 바로 2단계로 (건너뛰기 금지!)
- Wiki 파일 생성되었으면, **즉시** check.py 실행
- 다른 작업 먼저 하면 안 됨
- 2단계 통과 전에 3단계 실행 금지

### 2단계 (기계 검증)에서 실패하면
**진행 금지** → Wiki 수정 필요

- 오류 원인:
  - Frontmatter 누락 또는 형식 오류
  - 액션 테이블 컬럼 불일치
  - 파일명과 회의일 불일치
  - Raw 파일 참조 오류

- 수정 대상:
  - Wiki 파일 (Frontmatter, 구조)
  - 또는 Raw 파일 (회의일, 참석 불일치)

- 재검증:
  ```bash
  python check.py
  ```
  (오류 0개 확인까지 반복)

### 2단계 통과 후 3단계로 (순서 필수!)
- check.py가 모든 오류를 확인했으므로
- 2단계 통과 후에만 3단계 시작
- 2단계 통과 없이 3단계 실행 금지

### 3단계 (컨텐츠 검증)에서 경고가 나면
**진행 가능** → 검토 후 대응

- 검토: 경고 내용 확인
- 대응:
  - Raw 누락 → Raw에 정보 추가
  - Wiki 오류 → Wiki에서 수정
  - 액션 누락 → Raw에 상태 업데이트
- 재검증:
  ```bash
  /wiki-content-review wiki/YYYY-MM-DD-[회의체명].md
  ```

### 3단계 (컨텐츠 검증)에서 오류가 나면
**진행 금지** → Wiki 재생성 필요

- 원인 분석:
  - 임의 생성 콘텐츠?
  - Raw와 불일치?
  - 액션 추적 오류?
- 대응:
  - 1단계부터 다시 시작 (`/llm-wiki-ingest`)
  - Raw 수정 후 재생성
- 재검증: 1단계부터 반복

---

**상태**: 검증 워크플로우 순서 규칙 강화 (순차 진행 필수, 단계 건너뛰기 금지)  
**마지막 수정**: 2026-08-10

---

## 📌 규칙 요약

### 절대 규칙 (필수)
1. **0→1→2→3 순서 필수** (단계 건너뛰기 금지)
2. **1단계: 스킬 사용 필수** (수동 생성 금지)
3. **2단계: 오류 0개까지** (경고는 가능)
4. **3단계: 2단계 통과 후** (통과 전 실행 금지)

### 실패 시 처리
- **1단계 실패**: (실패 불가 - 스킬이 생성)
- **2단계 실패**: Wiki 수정 → 재검증
- **3단계 실패**: 1단계부터 재시작
