# LLM Wiki SSOT — 개발 규칙

## 핵심 원칙

1. **SSOT는 Raw에만** — Wiki는 파생 아티팩트
2. **액션 중심 설계** — 결정/액션/보류로 구성
3. **최소 구조 강제** — 프론트매터만 필수, 본문 자유
4. **파일명 규약** — `YYYY-MM-DD-[회의체명].md`

---

## ✅ Raw 회의록 작성 규칙

### 필수
1. 프론트매터 포함 (`회의일`, `참석`)
2. 파일명: `YYYY-MM-DD-[회의체명].md`
3. 결정을 `### 결정` 헤더로 명시
4. 액션을 `## 액션 아이템`에 체크박스로 기록
5. **이전 액션의 상태를 모두 업데이트**
6. 우선순위 변경 시 사유 명시
7. 외부 의존성 표시
8. 보류 항목에 해결 조건 기록

---

## ❌ 절대 피할 것

### Raw에서
- ❌ 이전 액션 상태 기록 안 함
- ❌ 프론트매터 없음
- ❌ 결정을 본문에 숨김
- ❌ 액션 상태 불명확
- ❌ 우선순위 변경 설명 없음
- ❌ 외부 의존성 숨김
- ❌ 보류 항목 방치

### 일반
- ❌ Raw와 Wiki 불일치 방치
- ❌ 의사결정 배경 기록 안 함
- ❌ Git 커밋 미루기

---

## ⚠️ 가장 중요한 2가지

1. **이전 액션 상태 업데이트** (Raw)
   ```markdown
   ## 이전 액션 상태 업데이트
   - [x] 완료된 액션
   - ⏳ 진행 중인 액션
   - ⏸️ 홀드된 액션
   ```

2. **Raw 작성이 모든 것의 기초**
   - Wiki는 Raw에서 자동 생성됨
   - Raw의 품질이 Wiki의 품질을 결정
   - 규칙을 따르는 Raw 작성이 핵심

---

## 🤖 Wiki 생성 후 검증 워크플로우

**4단계 프로세스**: Raw 적재 → Wiki 생성 → 기계 검증 → 컨텐츠 검증

### 1단계: Wiki 생성 (llm-wiki-ingest 스킬)

**명령**:
```bash
/llm-wiki-ingest raw/YYYY-MM-DD-[회의체명].md
```

**스킬이 처리**:
- Wiki 프론트매터 작성 (원본, 회의일, 참석 등)
- 결정/액션/보류 3계층 구조
- 액션 테이블 생성 (의존성 컬럼 포함)
- 고위험 액션 표시 (🚨)
- 이전 액션 추적 (완료/미해결 분리)
- 의존성 표시 (🟢/🔴/⚫)

**성공 기준**:
- [ ] Wiki 파일 생성됨
- [ ] Frontmatter 자동 작성
- [ ] 3계층 구조 생성

---

### 2단계: 기계 검증 (check.py 스크립트)

**명령**:
```bash
python check.py
```

**스크립트가 검증**:
- ✅ 파일명 규칙 (`YYYY-MM-DD-*.md`)
- ✅ Frontmatter 필드 (회의일, 참석 필수)
- ✅ 스키마 구조 (결정/액션/보류 섹션)
- ✅ 액션 테이블 컬럼 (`| # | 담당자 | 항목 | 마감 | 상태 | 의존성 |`)
- ✅ Frontmatter ↔ 파일명 일관성

**성공 기준**:
- [ ] 파일명 검증 통과
- [ ] Frontmatter 검증 통과
- [ ] 스키마 검증 통과
- [ ] 일관성 검증 통과
- [ ] 오류: 0개, 경고: 0개 (또는 사소한 수준)

**실패 시 대응**:
❌ 오류 발생 → Wiki 파일 수정 → 다시 실행

---

### 3단계: 컨텐츠 검증 (wiki-content-review 스킬)

**명령**:
```bash
/wiki-content-review wiki/YYYY-MM-DD-[회의체명].md
```

또는 일괄 검증:
```bash
/wiki-content-review all
```

**스킬이 검증**:
- 🔍 Raw ↔ Wiki 내용 일치도
- 🔍 임의 생성된 콘텐츠 감지
- 🔍 누락된 중요 내용 확인
- 🔍 액션 추적 (열림/진행/완료, 조용히 사라진 액션)
- 🔍 의존성 표시 (🟢/🔴/⚫)
- 🔍 고위험 표시 (🚨)

**검증 항목**:
- [ ] 결정사항이 Raw와 일치
- [ ] 액션 아이템이 Raw와 일치
- [ ] 이전 액션 상태가 추적됨
- [ ] 임의 생성 콘텐츠 없음
- [ ] 외부 의존성 표시됨
- [ ] 액션이 조용히 사라지지 않음

**성공 기준**:
- [ ] 통과 (오류 0개)
- [ ] 또는 경고만 있음 (경고는 확인 후 처리)

**실패 시 대응**:
⚠️  경고 발생 → Raw 또는 Wiki 수정 → 재검증
❌ 오류 발생 → Wiki 재생성 또는 Raw 수정

---

## 📋 완전한 워크플로우 체크리스트

### Raw 적재 후
- [ ] 파일명: `YYYY-MM-DD-[회의체명].md`
- [ ] Frontmatter: 회의일, 참석 필수
- [ ] 결정/액션/보류 섹션 포함
- [ ] 이전 액션 상태 업데이트 포함

### 1단계: Wiki 생성
```
/llm-wiki-ingest raw/YYYY-MM-DD-[회의체명].md
```
- [ ] Wiki 파일 생성 확인
- [ ] Frontmatter 자동 작성 확인

### 2단계: 기계 검증
```
python check.py
```
- [ ] 파일명 ✅
- [ ] Frontmatter ✅
- [ ] 스키마 ✅
- [ ] 일관성 ✅
- [ ] 오류 0개 확인

### 3단계: 컨텐츠 검증
```
/wiki-content-review wiki/YYYY-MM-DD-[회의체명].md
```
- [ ] Raw ↔ Wiki 일치도 확인
- [ ] 액션 추적 확인
- [ ] 의존성 표시 확인
- [ ] 경고/오류 검토

### 최종 확인
- [ ] Wiki 파일 생성됨 ✅
- [ ] 기계 검증 통과 ✅
- [ ] 컨텐츠 검증 통과 ✅
- [ ] Git 커밋 & 푸시

---

## 🚨 주요 검증 규칙

### 기계 검증에서 실패하면
- 수정 대상: Wiki 파일 (Frontmatter, 구조)
- 또는 Raw 파일 (회의일, 참석 불일치)
- 재검증: `node validate-wiki.js`

### 컨텐츠 검증에서 경고가 나면
- 검토: 경고 내용 확인
- 대응:
  - Raw 누락 → Raw에 정보 추가
  - Wiki 오류 → Wiki에서 수정
  - 액션 누락 → Raw에 상태 업데이트
- 재검증: `/wiki-content-review [파일명]`

### 컨텐츠 검증에서 오류가 나면
- 원인 분석:
  - 임의 생성 콘텐츠?
  - Raw와 불일치?
  - 액션 추적 오류?
- 대응:
  - Wiki 재생성 또는 수정
  - Raw 보완
- 재검증: 처음부터

---

**상태**: 검증 워크플로우 규칙 추가  
**마지막 수정**: 2026-08-10
