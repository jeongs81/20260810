# LLM Wiki SSOT — 개발 규칙

## 핵심 원칙

1. **SSOT는 Raw에만** — Wiki는 파생 아티팩트
2. **액션 중심 설계** — 결정/액션/보류로 구성
3. **최소 구조 강제** — 프론트매터만 필수, 본문 자유
4. **파일명 규약** — `YYYY-MM-DD-[회의체명].md`

---

## ✅ Raw 회의록 작성 규칙

### 필수
1. 프론트매터 포함 (`회의일`, `참석`)
2. 파일명: `YYYY-MM-DD-[회의체명].md`
3. 결정을 `### 결정` 헤더로 명시
4. 액션을 `## 액션 아이템`에 체크박스로 기록
5. **이전 액션의 상태를 모두 업데이트**
6. 우선순위 변경 시 사유 명시
7. 외부 의존성 표시
8. 보류 항목에 해결 조건 기록

---

## ❌ 절대 피할 것

### Raw에서
- ❌ 이전 액션 상태 기록 안 함
- ❌ 프론트매터 없음
- ❌ 결정을 본문에 숨김
- ❌ 액션 상태 불명확
- ❌ 우선순위 변경 설명 없음
- ❌ 외부 의존성 숨김
- ❌ 보류 항목 방치

### 일반
- ❌ Raw와 Wiki 불일치 방치
- ❌ 의사결정 배경 기록 안 함
- ❌ Git 커밋 미루기

---

## ⚠️ 가장 중요한 2가지

1. **이전 액션 상태 업데이트** (Raw)
   ```markdown
   ## 이전 액션 상태 업데이트
   - [x] 완료된 액션
   - ⏳ 진행 중인 액션
   - ⏸️ 홀드된 액션
   ```

2. **Raw 작성이 모든 것의 기초**
   - Wiki는 Raw에서 자동 생성됨
   - Raw의 품질이 Wiki의 품질을 결정
   - 규칙을 따르는 Raw 작성이 핵심

---

## 🤖 Wiki 생성은 Ingest 스킬이 담당

**Raw → Wiki 변환**: `/llm-wiki-ingest` 스킬 사용

스킬이 자동으로 처리하는 것:
- Wiki 프론트매터 작성
- 결정/액션/보류 3계층 구조
- 액션 테이블 생성 (의존성 컬럼 포함)
- 고위험 액션 표시 (🚨)
- 이전 액션 추적 (완료/미해결 분리)
- 의존성 표시 (🟢/🔴/⚫)
- 검증 및 오류 감지

**사용 예시**:
```bash
/llm-wiki-ingest raw/2026-05-14-제품주간회의.md
```

→ Wiki 파일이 자동 생성됨

---

**상태**: 규칙 확립 완료  
**마지막 수정**: 2026-08-10
